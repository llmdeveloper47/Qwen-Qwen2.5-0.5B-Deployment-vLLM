# INSTALLATION NOTES:
# For best compatibility, install in this order:
# 1. pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 --index-url https://download.pytorch.org/whl/cu121
# 2. pip install transformers==4.45.2 tokenizers==0.20.3
# 3. pip install -r requirements.txt (for remaining packages)
#
# Or use: pip install -r requirements.txt --ignore-installed blinker
#
# Note: ONNX Runtime removed (Qwen2 not supported for classification tasks)

# Core ML dependencies - CRITICAL: Must use these exact versions
# PyTorch 2.4.0 is stable (2.9.x has memory bugs, 2.5.x incompatible with optimum)
torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0

# Transformers - Compatible with optimum 1.16.2
transformers==4.45.2
tokenizers==0.20.3

# Datasets - Use 2.20.0 for compatibility with pandas/numpy
datasets==2.20.0

# Model optimization
peft==0.7.1
accelerate>=0.26.0

# Quantization support
bitsandbytes>=0.43.2

# Hugging Face Hub integration
huggingface-hub==0.23.5
safetensors==0.4.5

# Load testing
locust>=2.32.0

# Data processing and analysis
numpy>=1.26.4,<2.0.0
pandas>=2.1.0
scikit-learn>=1.3.2

# API and HTTP clients
requests>=2.32.3
aiohttp>=3.9.5

# Progress bars and CLI
tqdm>=4.66.0
click>=8.1.0

# Logging and monitoring
python-json-logger>=2.0.7

# Plotting and visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Jupyter for analysis notebooks
jupyter>=1.0.0
notebook>=7.0.0
ipywidgets>=8.1.0

# PDF report generation
reportlab>=4.0.0
Pillow>=10.1.0

# Configuration management
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Testing utilities
pytest>=7.4.0
pytest-asyncio>=0.21.0

